#!/usr/bin/env python
from setuptools import setup

if __name__ == '__main__':
    setup(name = "avalanche",
          version = "0.1",
          packages = ['avalanche'],
          package_dir = {'avalanche': ''})


#!/usr/bin/env python
from setuptools import setup

if __name__ == '__main__':
    setup(name = "avalanche",
          version = "0.2.0",
          packages = ['avalanche'],
          package_dir = {'avalanche': ''})
